apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-336ba837b515
  namespace: prod
spec:
  replicas: 3
  selector:
    matchLabels:
      app: app-336ba837b515
  template:
    metadata:
      labels:
        app: app-336ba837b515
    spec:
      containers:
      - image: nginx:alpine
        name: web
        ports:
        - containerPort: 80
        resources:
          limits:
            cpu: 500m
            memory: 512Mi
          requests:
            cpu: 250m
            memory: 256Mi
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
---
apiVersion: v1
kind: Service
metadata:
  name: app-336ba837b515-svc
  namespace: prod
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: app-336ba837b515
  type: ClusterIP
